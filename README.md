# Gerador de Certificados Escoteiros

App desktop em Python (Tkinter) para cadastrar membros e gerar certificados em PDF (3 por folha A4) usando template PNG.

## Como rodar
```bash
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
python app.py
